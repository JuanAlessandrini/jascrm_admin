{% extends 'base.html.twig' %}

{% block title %}AccountingGroup2{% endblock %}

{% block body %}
    <h1>AccountingGroup2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ accounting_group2.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ accounting_group2.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_accounting_group2_index') }}">back to list</a>

    <a href="{{ path('app_accounting_group2_edit', {'id': accounting_group2.id}) }}">edit</a>

    {{ include('accounting_group2/_delete_form.html.twig') }}
{% endblock %}
