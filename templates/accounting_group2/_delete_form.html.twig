<form method="post" action="{{ path('app_accounting_group2_delete', {'id': accounting_group2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accounting_group2.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
