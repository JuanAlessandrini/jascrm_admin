{% extends 'base.html.twig' %}

{% block title %}EntidadField{% endblock %}

{% block body %}
    <h1>EntidadField</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entidad_field.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entidad_field.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ entidad_field.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entidad_field_index') }}">back to list</a>

    <a href="{{ path('app_entidad_field_edit', {'id': entidad_field.id}) }}">edit</a>

    {{ include('entidad_field/_delete_form.html.twig') }}
{% endblock %}
