<form method="post" action="{{ path('app_sub_cuenta_delete', {'id': sub_cuentum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_cuentum.id) }}">
    <button class="btn">Delete</button>
</form>
