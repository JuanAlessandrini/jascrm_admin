{% extends 'base.html.twig' %}

{% block title %}SubCuenta{% endblock %}

{% block body %}
    <h1>SubCuenta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_cuentum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_cuentum.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sub_cuenta_index') }}">back to list</a>

    <a href="{{ path('app_sub_cuenta_edit', {'id': sub_cuentum.id}) }}">edit</a>

    {{ include('sub_cuenta/_delete_form.html.twig') }}
{% endblock %}
