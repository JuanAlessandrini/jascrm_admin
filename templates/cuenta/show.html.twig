{% extends 'base.html.twig' %}

{% block title %}Cuenta{% endblock %}

{% block body %}
    <h1>Cuenta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cuentum.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cuentum.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex"><a class="btn" href="{{ path('app_cuenta_index') }}">Volver</a>
    <a class="btn btn-primary" href="{{ path('app_cuenta_edit', {'id': cuentum.id}) }}">Editar</a>

    {{ include('cuenta/_delete_form.html.twig') }}
    </div>
{% endblock %}
