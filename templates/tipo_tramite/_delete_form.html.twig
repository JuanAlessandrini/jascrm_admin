<form method="post" action="{{ path('app_tipo_tramite_delete', {'id': tipo_tramite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_tramite.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
