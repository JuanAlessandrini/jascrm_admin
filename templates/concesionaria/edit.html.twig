
    <div class="modal-header">
        <h5 class="modal-title" id="modalLongTitle">Editar Concesionaria</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
        </button>
    </div>
    <div id="modalBody" class="modal-body">
        <form id="frmConcesionaria" action="{{path('app_concesionaria_edit', {id: concesionarium.id})}}" method="POST">
            {{ include('concesionaria/_form.html.twig', {'button_label': 'Update'}) }}
        </form>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-secondary pull-left" data-dismiss="modal">Cerrar</button>
        <button type="button" onclick="document.forms['frmConcesionaria'].submit();" class="btn btn-success"><i class="material-icons align-center">save</i> Guardar</button>
        {{ include('concesionaria/_delete_form.html.twig') }}
    </div>

<script>
  var sucursales = new Choices('.js-choice',{
    allowHTML: true,
    paste: false,
    duplicateItemsAllowed: false,
    delimiter: ',',
    editItems: true,
    removeItemButton: true,
    });
</script>