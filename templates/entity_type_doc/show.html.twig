{% extends 'base.html.twig' %}

{% block title %}EntityTypeDoc{% endblock %}

{% block body %}
    <h1>EntityTypeDoc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity_type_doc.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity_type_doc.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ entity_type_doc.code }}</td>
            </tr>
            <tr>
                <th>Signo</th>
                <td>{{ entity_type_doc.signo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entity_type_doc_index') }}">back to list</a>

    <a href="{{ path('app_entity_type_doc_edit', {'id': entity_type_doc.id}) }}">edit</a>

    {{ include('entity_type_doc/_delete_form.html.twig') }}
{% endblock %}
