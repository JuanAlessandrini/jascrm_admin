
    {{ form_widget(form) }}

    <script>
  // Note that the name "myDropzone" is the camelized
  // id of the form.
$(document).ready((){
  //$("#customer_cuit").inputmask({"mask": "##-########-#"});
});
  
  
  var provincias = new Choices('.js-choice-select',{
    allowHTML: false,
    paste: false,
    duplicateItemsAllowed: false,
    editItems: false,
    removeItemButton: true,
    });
</script>
  