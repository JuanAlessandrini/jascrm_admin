<form method="post" action="{{ path('app_entidad_type_doc_delete', {'id': entidad_type_doc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entidad_type_doc.id) }}">
    <button class="btn">Delete</button>
</form>
