<form method="post" id="grano_delete" action="{{ path('app_grano_delete', {'id': grano.id}) }}" style="display:none" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grano.id) }}">
    <button class="btn">Delete</button>
</form>
