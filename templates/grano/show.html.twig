{% extends 'base.html.twig' %}

{% block title %}Grano{% endblock %}

{% block body %}
    <h1>Grano</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grano.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ grano.name }}</td>
            </tr>
            <tr>
                <th>Short</th>
                <td>{{ grano.short }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_grano_index') }}">back to list</a>

    <a href="{{ path('app_grano_edit', {'id': grano.id}) }}">edit</a>

    {{ include('grano/_delete_form.html.twig') }}
{% endblock %}
