{% extends 'dashboard/dashboard_base.html.twig' %}

{% block title %}CuentaAuxiliar index{% endblock %}

{% block content %}
    <h1>CuentaAuxiliar index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cuenta_auxiliar in cuenta_auxiliars %}
            <tr>
                <td>{{ cuenta_auxiliar.id }}</td>
                <td>{{ cuenta_auxiliar.name }}</td>
                <td>
                    <a href="{{ path('app_cuenta_auxiliar_show', {'id': cuenta_auxiliar.id}) }}">show</a>
                    <a href="{{ path('app_cuenta_auxiliar_edit', {'id': cuenta_auxiliar.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_cuenta_auxiliar_new') }}">Create new</a>
{% endblock %}
