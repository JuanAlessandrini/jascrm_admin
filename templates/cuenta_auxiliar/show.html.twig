{% extends 'base.html.twig' %}

{% block title %}CuentaAuxiliar{% endblock %}

{% block body %}
    <h1>CuentaAuxiliar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cuenta_auxiliar.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cuenta_auxiliar.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cuenta_auxiliar_index') }}">back to list</a>

    <a href="{{ path('app_cuenta_auxiliar_edit', {'id': cuenta_auxiliar.id}) }}">edit</a>

    {{ include('cuenta_auxiliar/_delete_form.html.twig') }}
{% endblock %}
