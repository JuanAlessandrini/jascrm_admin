{% extends 'dashboard/dashboard_base.html.twig' %}

{% block title %}SubCuentas{% endblock %}

{% block content %}
    <h4>Usuarios</h4>
    <div class="card card-body">
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Email</th>
                    <th>Nombre</th>
                    <th>Apellido</th>
                    <th>Clientes</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.firstName }}</td>
                    <td>{{ user.lastName }}</td>
                    <td>
                        {% for cliente in user.clientes %}
                            <span class="badge bg-primary">{{cliente.name}}</span>
                        {% else %}
                            <span class="badge bg-label-success">Todos</span>
                        {% endfor %}
                    </td>
                    <td>
                        
                        <a onclick="showFormLg('{{ path('app_user_edit', {'id': user.id}) }}')"><i class='bx bxs-edit-alt' ></i></a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="8">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div  class="btn btn-primary"><a onclick="showFormLg('{{ path('app_user_new') }}')">Nuevo</a></div>
        </div>
{% endblock %}
