<form method="post" action="{{ path('app_tramite_delete', {'id': tramite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tramite.id) }}">
    <button class="btn">Delete</button>
</form>
